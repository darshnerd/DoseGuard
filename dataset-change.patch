# ============================================================
# DoseGuard — 'Real drug-interaction dataset' change (complete diff set)
# Generated from working tree state on 1a8cb76 (uncommitted)
# NOTE: raw data files under app/data/sources/ are excluded (tens of MB);
#       they are listed at the end. Everything else is a full diff below.
# ============================================================

##### [MODIFIED] dependency + config #####
diff --git a/packages/api/app/config.py b/packages/api/app/config.py
index 82439c6..7b08890 100644
--- a/packages/api/app/config.py
+++ b/packages/api/app/config.py
@@ -22,6 +22,8 @@ class Settings(BaseSettings):
     refresh_token_days: int = 7
     max_upload_mb: int = 5
 
+    pubchem_live: bool = True
+
     @model_validator(mode="after")
     def _require_secret_in_prod(self):
         if self.environment == "production":
diff --git a/packages/api/pyproject.toml b/packages/api/pyproject.toml
index 5215431..2627b8b 100644
--- a/packages/api/pyproject.toml
+++ b/packages/api/pyproject.toml
@@ -16,7 +16,8 @@ dependencies = [
     "pyjwt>=2.13.0",
     "bcrypt>=5.0.0",
     "slowapi>=0.1.9",
-    "email-validator>=2.3.0"
+    "email-validator>=2.3.0",
+    "rapidfuzz>=3.14.5"
 ]
 
 [project.optional-dependencies]

##### [MODIFIED] app/models.py (new drug tables + Interaction rewrite) #####
diff --git a/packages/api/app/models.py b/packages/api/app/models.py
index 7ab4f65..524e4b1 100644
--- a/packages/api/app/models.py
+++ b/packages/api/app/models.py
@@ -1,15 +1,48 @@
-from datetime import UTC, datetime
+from datetime import UTC, date, datetime
 
 from sqlalchemy import JSON, Column
 from sqlmodel import Field, SQLModel
 
+class DrugConcept(SQLModel, table=True):
+    id: int | None = Field(default=None, primary_key=True)
+    canonical_name: str
+    normalized_name: str = Field(index=True, unique=True)
+    rxcui: str | None = None
+
+
+class DrugAlias(SQLModel, table=True):
+    id: int | None = Field(default=None, primary_key=True)
+    alias_normalized: str = Field(index=True)
+    concept_normalized: str = Field(index=True)
+    country: str | None = None
+    source: str = "unknown"
+
+
+class MedicineProduct(SQLModel, table=True):
+    id: int | None = Field(default=None, primary_key=True)
+    name: str
+    name_normalized: str = Field(index=True)
+    manufacturer: str | None = None
+    country: str = "IN"
+    source: str = "unknown"
+
+
+class ProductIngredient(SQLModel, table=True):
+    id: int | None = Field(default=None, primary_key=True)
+    product_id: int = Field(index=True, foreign_key="medicineproduct.id")
+    ingredient_normalized: str = Field(index=True)
+    strength: str | None = None
 
 class Interaction(SQLModel, table=True):
     id: int | None = Field(default=None, primary_key=True)
-    ingredient_a: str = Field(index=True)
-    ingredient_b: str = Field(index=True)
-    severity: str
-    description: str
+    a_norm: str = Field(index=True)
+    b_norm: str = Field(index=True)
+    severity: str = "moderate"
+    description: str | None = None
+    mechanism: str | None = None
+    management: str | None = None
+    references_json: str | None = None
+    sources: str = ""
 
 class User(SQLModel, table=True):
     id: int | None = Field(default=None, primary_key=True)
@@ -26,6 +59,8 @@ class Medication(SQLModel, table=True):
     id: int | None = Field(default=None, primary_key=True)
     user_id: int = Field(index=True, foreign_key="user.id")
     name: str
+    start_date: date = Field(default_factory=lambda: datetime.now(UTC).date())
+    duration_days: int | None = None
 
 class MedicationIngredient(SQLModel, table=True):
     id: int | None = Field(default=None, primary_key=True)

##### [NEW] code files (full content as additions) #####
diff --git a/packages/api/app/normalize.py b/packages/api/app/normalize.py
new file mode 100644
index 0000000..53f7b50
--- /dev/null
+++ b/packages/api/app/normalize.py
@@ -0,0 +1,18 @@
+import re
+
+CANONICAL_ALIASES = {
+    "paracetamol": "acetaminophen", "acetylsalicylic acid": "aspirin",
+    "amoxycillin": "amoxicillin", "salbutamol": "albuterol",
+    "levosalbutamol": "levalbuterol", "frusemide": "furosemide",
+    "rifampicin": "rifampin", "cetrizine": "cetirizine",
+}
+_STRENGTH = re.compile(r"\(.*?\)|\d+\s*(mg|mcg|ml|g|iu|%)\b", re.I)
+_NONALPHA = re.compile(r"[^a-z0-9 ]+")
+
+
+def normalize(name: str) -> str:
+    if not name:
+        return ""
+    s = _NONALPHA.sub(" ", _STRENGTH.sub(" ", name.casefold().strip()))
+    s = " ".join(s.split())
+    return CANONICAL_ALIASES.get(s, s)
\ No newline at end of file
diff --git a/packages/api/app/services/normalization.py b/packages/api/app/services/normalization.py
new file mode 100644
index 0000000..78cdfca
--- /dev/null
+++ b/packages/api/app/services/normalization.py
@@ -0,0 +1,53 @@
+from rapidfuzz import process
+from sqlmodel import Session, select
+
+from app.models import DrugAlias, DrugConcept, MedicineProduct, ProductIngredient
+from app.normalize import normalize
+from app.services.rxnorm import RxNormClient
+
+
+class Resolved:
+    def __init__(self, ingredient: str, confidence: float, source: str):
+        self.ingredient = ingredient
+        self.confidence = confidence
+        self.source = source
+
+
+async def resolve_term(session: Session, text_in: str) -> list[Resolved]:
+    n = normalize(text_in)
+    if not n:
+        return []
+
+    prod = session.exec(select(MedicineProduct).where(MedicineProduct.name_normalized == n)).first()
+    if prod:
+        rows = session.exec(
+            select(ProductIngredient).where(ProductIngredient.product_id == prod.id)
+        ).all()
+        if rows:
+            return [Resolved(r.ingredient_normalized, 1.0, "product") for r in rows]
+
+    alias = session.exec(select(DrugAlias).where(DrugAlias.alias_normalized == n)).first()
+    if alias:
+        return [Resolved(alias.concept_normalized, 0.95, "alias")]
+    if session.exec(select(DrugConcept).where(DrugConcept.normalized_name == n)).first():
+        return [Resolved(n, 1.0, "concept")]
+
+    names = session.exec(select(DrugConcept.normalized_name)).all()
+    m = process.extractOne(n, names, score_cutoff=88)
+    if m:
+        return [Resolved(m[0], m[1] / 100, "fuzzy")]
+
+    r = await RxNormClient().resolve(text_in)
+    if r.matched and r.ingredient_name:
+        return [Resolved(normalize(r.ingredient_name), 0.7, "rxnorm")]
+    return [Resolved(n, 0.2, "unknown")]
+
+
+async def resolve_terms(session: Session, texts: list[str]) -> list[Resolved]:
+    out, seen = [], set()
+    for t in texts:
+        for res in await resolve_term(se
+            if res.ingredient not in seen:
+                seen.add(res.ingredient)
+                out.append(res)
+    return out
\ No newline at end of file
diff --git a/packages/api/app/services/interaction_engine.py b/packages/api/app/services/interaction_engine.py
new file mode 100644
index 0000000..7879fc9
--- /dev/null
+++ b/packages/api/app/services/interaction_engine.py
@@ -0,0 +1,31 @@
+from itertools import combinations
+
+from sqlmodel import Session, select
+
+from app.models import Interaction
+
+RANK = {"contraindicated": 0, "severe": 1, "moderate": 2, "low": 3}
+
+
+def check(session: Session, ingredients: list[str]) -> list[dict]:
+    norm = sorted({i for i in ingredients if i})
+    results = []
+    for a, b in combinations(norm, 2):
+        a, b = sorted((a, b))
+        rows = session.exec(
+            select(Interaction).where(Interaction.a_norm == a, Interaction.b_norm == b)
+        ).all()
+        if not rows:
+            continue
+        rows.sort(key=lambda r: RANK.get(r.severity, 9))
+        top = rows[0]
+        results.append({
+            "ingredient_a": a, "ingredient_b": b,
+            "severity": top.severity,
+            "description": next((r.description for r in rows if r.description), None),
+            "mechanism": next((r.mechanism for r in rows if r.mechanism), None),
+            "source": top.sources,
+        })
+    results.sort(key=lambda r: RANK.get(r["severity"], 9))
+    return results
+    
\ No newline at end of file
diff --git a/packages/api/app/services/pubchem.py b/packages/api/app/services/pubchem.py
new file mode 100644
index 0000000..df2504a
--- /dev/null
+++ b/packages/api/app/services/pubchem.py
@@ -0,0 +1,83 @@
+import asyncio
+import csv
+import gzip
+import io
+import json
+from urllib.parse import urlencode
+
+import httpx
+
+from app.normalize import normalize
+
+_ENDPOINT = "https://pubchem.ncbi.nlm.nih.gov/sdq/sdqagent.cgi"
+
+
+def _build_url(field: str, value: str, limit: int) -> str:
+    query = {
+        "download": "*",
+        "collection": "drugbankddi",
+        "start": 1,
+        "limit": limit,
+        "where": {"ands": [{field: value}]},
+    }
+    return _ENDPOINT + "?" + urlencode({
+        "infmt": "json",
+        "outfmt": "csv",
+        "query": json.dumps(query, separators=(",", ":")),
+    })
+
+
+def _decode(content: bytes) -> str:
+    if content.startswith(b"\x1f\x8b"): 
+        content = gzip.decompress(content)
+    return content.decode("utf-8", errors="replace")
+
+
+async def _fetch(client: httpx.AsyncClient, field: str, value: str, limit: int) -> list[dict]:
+    try:
+        resp = await client.get(_build_url(field, value, limit))
+        if resp.status_code >= 400:
+            return []
+        return list(csv.DictReader(io.StringIO(_decode(resp.content))))
+    except Exception:
+        return []
+
+
+async def live_pairs(names: list[str], timeout: float = 4.0, limit: int = 20000) -> list[dict]:
+    wanted = {normalize(n) for n in names if n}
+    if len(wanted) < 2:
+        return []
+
+    async def _gather() -> list[list[dict]]:
+        async with httpx.AsyncClient(timeout=timeout, follow_redirects=True) as client:
+            tasks = []
+            for name in names:
+                tasks.append(_fetch(client, "synonym", name, limit))
+                tasks.append(_fetch(client, "name2", name, limit))
+            return await asyncio.gather(*tasks)
+
+    try:
+        pages = await asyncio.wait_for(_gather(), timeout + 1)
+    except Exception: 
+        return []
+
+    out: list[dict] = []
+    seen: set[tuple[str, str]] = set()
+    for rows in pages:
+        for row in rows:
+            a = normalize(row.get("synonym") or "")
+            b = normalize(row.get("name2") or "")
+            if a not in wanted or b not in wanted or a == b:
+                continue
+            key = tuple(sorted((a, b)))
+            if key in seen:
+                continue
+            seen.add(key)
+            out.append({
+                "ingredient_a": key[0],
+                "ingredient_b": key[1],
+                "severity": "moderate",
+                "description": (row.get("descr") or "").strip() or None,
+                "source": "PubChem",
+            })
+    return out
diff --git a/packages/api/app/services/search.py b/packages/api/app/services/search.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/scripts/_common.py b/packages/api/scripts/_common.py
new file mode 100644
index 0000000..cea0307
--- /dev/null
+++ b/packages/api/scripts/_common.py
@@ -0,0 +1,22 @@
+import sqlite3
+import sys
+from pathlib import Path
+
+API = Path(__file__).resolve().parent.parent
+sys.path.insert(0, str(API))
+from app.normalize import normalize
+
+DB = str(API / "doseguard.db")
+SOURCES = API / "app" / "data" / "sources"
+
+SEV_NAME = {"major": "severe", "moderate": "moderate", "minor": "low"}
+SEV_RANK = {"contraindicated": 0, "severe": 1, "major": 1, "moderate": 2, "minor": 3, "low": 3}
+
+def connect():
+    c = sqlite3.connect(DB); 
+    c.execute("PRAGMA synchronous=OFF"); 
+    c.execute("PRAGMA journal_mode=WAL"); 
+    return c
+
+def worse(a, b):
+    return a if SEV_RANK.get(a, 9) <= SEV_RANK.get(b, 9) else b
diff --git a/packages/api/scripts/ingest_ddinter.py b/packages/api/scripts/ingest_ddinter.py
new file mode 100644
index 0000000..fdb2500
--- /dev/null
+++ b/packages/api/scripts/ingest_ddinter.py
@@ -0,0 +1,12 @@
+import csv
+
+from _common import SEV_NAME, SOURCES
+
+
+def read():
+    with open(SOURCES / "ddinter.csv", encoding="utf-8") as f:
+        for r in csv.DictReader(f):
+            yield r["Drug_A"], r["Drug_B"], {
+                "severity": SEV_NAME.get((r["Level"] or "").lower(), "moderate"),
+                "source": "DDInter",
+            }
\ No newline at end of file
diff --git a/packages/api/scripts/ingest_descriptions.py b/packages/api/scripts/ingest_descriptions.py
new file mode 100644
index 0000000..8ceda69
--- /dev/null
+++ b/packages/api/scripts/ingest_descriptions.py
@@ -0,0 +1,11 @@
+import csv
+
+from _common import SOURCES
+
+
+def read():
+    with open(SOURCES / "ddi_descriptions.csv", encoding="utf-8") as f:
+        for r in csv.DictReader(f):
+            yield r["Drug 1"], r["Drug 2"], {
+                "description": r["Interaction Description"], "source": "DrugBank",
+            }
\ No newline at end of file
diff --git a/packages/api/scripts/build_dataset.py b/packages/api/scripts/build_dataset.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/scripts/enrich_pubchem.py b/packages/api/scripts/enrich_pubchem.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/scripts/ingest_curated.py b/packages/api/scripts/ingest_curated.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/scripts/ingest_indian.py b/packages/api/scripts/ingest_indian.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/scripts/ingest_mechanisms.py b/packages/api/scripts/ingest_mechanisms.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/tests/test_resolver.py b/packages/api/tests/test_resolver.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/api/tests/test_interactions_real.py b/packages/api/tests/test_interactions_real.py
new file mode 100644
index 0000000..e69de29
diff --git a/packages/web/src/components/DrugSearch.tsx b/packages/web/src/components/DrugSearch.tsx
new file mode 100644
index 0000000..e69de29
